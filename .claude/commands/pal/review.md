Use the PAL review tool on: $ARGUMENTS

Call `mcp__pal__review` with `model="mimo"`. Choose `review_type` based on the request:
- `review_type`: `full` (default) · `security` · `performance` · `quick`

Default to `full` review if not specified.
