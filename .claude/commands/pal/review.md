Use the PAL review tool on: $ARGUMENTS

Call `mcp__pal__review` with `mode="review"` and `model="mimo"`. Choose parameters based on the request:
- `review_type`: `full` (default) · `security` · `performance` · `quick`
- `severity_filter`: `all` (default) · `critical` · `high` · `medium` · `low`

Default to `full` review if not specified.
