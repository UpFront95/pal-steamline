Use the PAL cleanup tool on: $ARGUMENTS

Call `mcp__pal__review` with `mode="cleanup"` and `model="mimo-v2.5-pro"`. Choose `refactor_type` based on the request:
- `codesmells` (default) — anti-patterns and bad practices
- `decompose` — break up large functions/classes
- `modernize` — update to current language idioms
- `organization` — restructure file/module layout

Default to `codesmells` if not specified.
